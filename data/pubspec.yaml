name: data
description: Data-layer for TODO-list app.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.2.3

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.8.1
  uuid: ^4.2.2
  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  json_serializable: ^6.7.1
  build_runner: ^2.4.7
